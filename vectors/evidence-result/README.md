# Evidence Result v0 — example fixtures

Generated by `../../schemas/generate_result_examples.py`, checked by
`../../schemas/check_evidence_result_examples.py`. See `spec/evidence-result-v0.md` for the
normative text these fixtures illustrate — **sections 1–3 of that document are DRAFT**, pending
Steven's sign-off per this schema's gate; these fixtures are built to that draft and are
themselves DRAFT until his sign-off line lands in `action-state-ops/spec/outbox.md`.

Every digest in every file here is real: computed by `agent_action_capsule.canonical.json_digest`
(lowercase-hex SHA-256 of `UTF8(JCS(value))`, this repository's own canonicalization) over the
exact placeholder content object named next to it in `generate_result_examples.py`, never
hand-typed. Regenerate with `python3 ../../schemas/generate_result_examples.py` from this
directory's parent's parent to reproduce byte-identical output (the generator is a pure function
of the literal content objects in that script — no wall-clock or random input).

`OO` is the synthetic anchor/prospect placeholder throughout, per the 2026-09-22 ruling; no
fixture here or in the generator names a company.

## Positive fixture

`pos-oo-claims-result.json` — one synthetic OO claims Result, three claims, one per verdict
bucket, so `aggregate.buckets` is fully populated and the sponsor-facing "three buckets, never a
single number" rule (spec section 3) is exercised in the same fixture as the disclosure-policy
gate (spec section 2):

| Claim | `sufficiency` | `verdict` | bucket | `presentation.kind` | `presentation.status` |
|---|---|---|---|---|---|
| `claim-1` | `SATISFIED` | `met` | `met` | `disclosure` | `SATISFIED` |
| `claim-2` | `SATISFIED` | `not_met` | `not_met` | `disclosure` | `SATISFIED` — a disclosed failure |
| `claim-3` | `GAP` | `not_evaluable` | `not_evaluable` | `analysis` | `WITHHELD` — the disclosure gate case: `disclosure` is not legal here (spec section 2) |

`aggregate.coverage` reports `evaluated_population: 3` (the three claims above),
`excluded_not_applicable: 1` (one requirement excluded from the evaluated population entirely, not
represented by a claim — spec section 1's `Sufficiency` `NOT_APPLICABLE` mapping), and
`unknown_count: 0`.

## Negative fixtures

Each is a byte-for-byte copy of `pos-oo-claims-result.json` with exactly one field changed, and
each MUST fail `$defs/EvidenceResult` validation for exactly one documented reason:

| File | Mutation | Rule violated |
|---|---|---|
| `neg-untiered-claim.json` | `claims[0].tier` removed | `Claim` requires `tier` (spec section 1 — "every claim also carries `tier`") |
| `neg-met-with-sufficiency-gap.json` | `claims[0].sufficiency` changed `SATISFIED` → `GAP`, `verdict` left as `met` | Spec section 1's binding rule: `verdict: met`/`not_met` is legal only when `sufficiency: SATISFIED` |
| `neg-aggregate-without-coverage.json` | `aggregate.coverage` removed | `Aggregate` requires `coverage` (spec section 3 — "an aggregate without a coverage statement... fails to validate") |
| `neg-contract-ref-missing.json` | `claims[0].contract_ref` removed | `Claim` requires `contract_ref` (spec section 1) |
| `neg-disclosure-carrier-under-withheld.json` | `claims[2].presentation` changed from `analysis`/`WITHHELD` to `disclosure`/`WITHHELD` | Spec section 2's disclosure-policy gate: `disclosure` is not a legal carrier when `status` is `WITHHELD` or `NOT_COMMITTED` |

`check_evidence_result_examples.py` asserts each rejection directly, and additionally proves each
rejection is load-bearing (not vacuous) by removing the specific schema rule it depends on in
memory, confirming that SAME fixture then validates clean, and restoring the rule.

## Reproducing the validation run

```
cd agent-action-capsule   # repo root
python3 schemas/check_evidence_result_examples.py
```

Requires the `jsonschema` PyPI package (dev-time verification tool only; not a runtime dependency
of the `agent_action_capsule` package — see that package's `pyproject.toml`, which stays
stdlib-only by design).
